function Spinner() {
    return (
        <div className="status-message loading" role="status">
            <span className="spinner" />
            <span>Loading...</span>
        </div>
    );
};

export default Spinner;
